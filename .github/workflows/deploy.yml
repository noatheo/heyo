name: Testing the github action workflow

on: [push]
jobs:
  
  Explore-Github-Actions:
    runs-on: ubuntu-latest
    steps:
    - run: git branche 
  
  destroy:
    runs-on: ubuntu-latest
    steps:
    - run: echo "git branche"

  

